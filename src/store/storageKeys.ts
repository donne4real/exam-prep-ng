// Centralised localStorage keys. Changing the app name in one place
// updates all persisted stores.

export const STORAGE_KEYS = {
  progress: 'exam-prep-ng-progress',
  settings: 'exam-prep-ng-settings',
} as const;
